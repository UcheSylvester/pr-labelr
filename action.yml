name: 'Label new PR requests'
description: 'Automatically label new PR requests'
author: 'Uchenna Okoro'

inputs:
  github-token:
    description: 'GitHub token for authentication'
    required: true
  label:
    description: 'Label to add to new PR requests'
    required: true

runs:
  using: 'node16'
  main: 'dist/index.js'

branding:
  icon: 'bookmark'
  color: 'green'
